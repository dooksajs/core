import dsStarterTemplate from './src/templates/ds-starter.html'

export default {
  templates: [
    dsStarterTemplate
  ],
  setup: {
    count: 0
  },
  devDependencies: {
    setup: {
      // overwrite setup options
    },
    plugins: {
      // include new or overwrite plugins
    }
  }
}
