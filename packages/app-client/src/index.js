import {
  addComponentButton,
  anchor,
  button,
  card,
  cardBody,
  cardImg,
  cardTitle,
  container,
  div,
  editSectionInner,
  editSectionOuter,
  h1,
  h2,
  hr,
  icon,
  modal,
  modalBody,
  modalContent,
  modalDialog,
  modalFooter,
  modalHeader,
  modalSectionEdit,
  modalTitle,
  root,
  text
} from '@dooksa/components'
import createApp from '@dooksa/create-app'
import {
  $fetch,
  action,
  component,
  content,
  data,
  event,
  list,
  metadata,
  operator,
  page,
  query,
  route,
  token
} from '@dooksa/plugins'

export default createApp({
  plugins: [
    data,
    action,
    content,
    component,
    event,
    $fetch,
    list,
    metadata,
    operator,
    route,
    token,
    query,
    page
  ],
  components: [
    addComponentButton,
    anchor,
    button,
    card,
    cardBody,
    cardImg,
    cardTitle,
    container,
    div,
    editSectionInner,
    editSectionOuter,
    h1,
    h2,
    hr,
    icon,
    modal,
    modalBody,
    modalContent,
    modalDialog,
    modalFooter,
    modalHeader,
    modalSectionEdit,
    modalTitle,
    root,
    text
  ]
})
