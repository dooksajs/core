# Dooksa app

This bootstraps a dooksa instance 

### Setup config file

Edit plugins **name** and **filename** in **ds.plugin.config.js**

### Install dependencies

```
$ npm install
```

### Development without hot reload at localhost:8080

```
$ npm run dev
```

### Build with hash
This will build the external plugin

```
$ npm run build:hash
```