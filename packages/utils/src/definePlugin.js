/**
 * @typedef {import('./types.js').DsPluginData} DsPluginData
 */

/**
 * @param {DsPluginData} plugin
 * @returns {DsPluginData}
 */
function definePlugin (plugin) {
  return plugin
}

export default definePlugin
