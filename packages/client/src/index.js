import {
  action,
  component,
  content,
  data,
  event,
  $fetch,
  layout,
  list,
  metadata,
  operator,
  page,
  query,
  router,
  template,
  token,
  section,
  view,
  widget
} from '@dooksa/plugins'
import createApp from '@dooksa/create-app'

export default createApp([
  data,
  action,
  component,
  content,
  event,
  $fetch,
  layout,
  list,
  metadata,
  operator,
  router,
  template,
  token,
  query,
  view,
  page,
  section,
  widget
])
