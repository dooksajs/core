import {
  action,
  component,
  content,
  data,
  event,
  $fetch,
  layout,
  list,
  metadata,
  operator,
  router,
  template,
  token,
  query,
  view
} from '@dooksa/plugins'
import { createApp } from '@dooksa/create'

export default createApp([
  data,
  action,
  component,
  content,
  event,
  $fetch,
  layout,
  list,
  metadata,
  operator,
  router,
  template,
  token,
  query,
  view
])
